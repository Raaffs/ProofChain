package main_test

import "testing"
func TestEncrypt(t *testing.T) {
}
// for doc range docs{
//  userAddr=getUserAddr(doc.requester)
// 	SetMultiSigKey(userAddr)
// 	secret,err=GenerateSecret()
// 	doc.ipfshash=DecryptIPFSHash(secret, doc.ipfshash)
// }

//get user's public key
//use that key to generate shared secret
//use shared key to retrieve ipfs addr